# Deployment

This repository includes an AWS SAM template in `template.yaml`.

Suggested flow:

```powershell
sam build
sam deploy --guided
```

Configure required environment variables and secrets before enabling scheduled runs.

