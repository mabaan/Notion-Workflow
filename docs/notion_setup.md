# Notion Setup

Create two Notion databases:

- Articles database for collected and enriched article records.
- Drafts database for generated newsletters and social drafts.

Store the database IDs in `.env` using the keys from `.env.example`.

